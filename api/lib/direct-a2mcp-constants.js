export const DIRECT_QUOTE_TTL_SECONDS = 10 * 60;
export const MAX_DIRECT_AUTHORIZATION_WINDOW_SECONDS = 15 * 60;
export const MIN_DIRECT_EXECUTION_WINDOW_SECONDS = 30;

export const MAX_DIRECT_ENROLLMENT_WINDOW_SECONDS = Math.min(
  DIRECT_QUOTE_TTL_SECONDS,
  MAX_DIRECT_AUTHORIZATION_WINDOW_SECONDS,
) - MIN_DIRECT_EXECUTION_WINDOW_SECONDS;
