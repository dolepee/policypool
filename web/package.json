{
  "name": "policypool-web",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html home.html coverage.html ledger.html proof.html providers.html enroll.html receipt.html styles.css tokens.css coverage-site.css app.js coverage-site.js enroll.js receipt.js favicon.svg logo.svg og.svg og.png agent-og.png agent-avatar.svg agent-avatar-v2.png robots.txt sitemap.xml dist/"
  }
}
