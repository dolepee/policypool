{
  "name": "policypool-web",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html agent.html styles.css tokens.css app.js agent.js favicon.svg logo.svg og.svg og.png agent-og.png agent-avatar.svg dist/"
  }
}
